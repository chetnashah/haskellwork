
What is a Monad Transformer?
monad transformers: special types that allow us to roll/combine two monads into a single one that shares the behavior of both.


Transformer Ends with `T` e.g.
`ReaderT`, `StateT`, `ExceptT`.

